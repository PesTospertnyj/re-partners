# RE Partners Pack API

Small Go service for managing pack sizes and calculating best pack combination for order quantity.

## Run with Docker

```bash
docker compose up --build
```

API runs on `http://localhost:8080`, Swagger UI on `http://localhost:8080/api/index.html`.

## Run tests

```bash
go test ./...
```

Or with Make:

```bash
make test
```
